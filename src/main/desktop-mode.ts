import { BrowserWindow } from 'electron'

export function enableDesktopMode(_win: BrowserWindow): void {
  // no-op: desktop mode removed
}

export function disableDesktopMode(_win: BrowserWindow): void {
  // no-op: desktop mode removed
}
